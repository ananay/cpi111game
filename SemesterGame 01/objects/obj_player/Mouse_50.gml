/*
var spawn_x = 0;
var spawn_y = 0;
/*
if(image_index == 4) //looking down
{
	
}

bullet_origin_x = self.x
bullet_origin_y = self.y
//borrowed from the topDown script
if(canShoot == true)
{
	var length = point_distance(x,y,bullet_origin_x,bullet_origin_y)
	var g_angle = point_direction(x,y,bullet_origin_x,bullet_origin_y)
	
	spawn_x = x + lengthdir_x(length, g_angle+image_angle);
	spawn_y = y + lengthdir_y(length, g_angle+image_angle);
		
	
	//need to finish calculating spawn_x/y
	var bullet_obj = instance_create_layer(spawn_x,spawn_y,"Instances",obj_player_bullet)
	
	bullet_obj.hspeed = velocity_x * 3
	bullet_obj.vspeed = velocity_y * 3
	bullet_obj.image_angle = image_angle
	
	canShoot = false
	alarm[9] = room_speed / 3;
}
*/
